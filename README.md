# Notifications API
Simula envio de e-mails via log no console.
- `UserCreatedEvent` -> boas-vindas
- `PaymentProcessedEvent` -> confirmação de compra quando Approved

## Variáveis de ambiente
- RabbitMQ__Host (default: rabbitmq)
# fcg-notifications-api
